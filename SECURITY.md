# 安全问题

## 支持范围

项目处于早期开发阶段，只维护最新发布版本和 `main` 分支。旧版本发现的问题应先在最新版本确认。

## 报告方式

安全问题不要提交公开 Issue，也不要在日志中公开桌面画面、文件路径或其他敏感信息。

请在 GitHub 仓库的 `Security` 页面选择 `Report a vulnerability`，通过私有漏洞报告提供：

- 受影响版本或提交；
- 操作系统、桌面环境和显示协议；
- 可复现的最小步骤；
- 实际影响；
- 必要的日志或示例文件。